parametres = tuning.best_params

model = XGBClassifier(
    **parametres,
    n_estimators=800,
    eval_metric="logloss",
    random_state=42,
    n_jobs=-1
)

pipeline = Pipeline([
    ("preprocessor", preprocessor),
    ("model", model)
])

pipeline.fit(X, y_enc)
